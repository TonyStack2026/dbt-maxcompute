# Pure-Python wheels only: the resulting zip is imported directly by the
# MaxCompute CPython 3.11 UDF runtime.
jmespath==1.1.0
python-dateutil==2.9.0.post0
six==1.17.0
phonenumbers==9.0.36
text-unidecode==1.3
